{
  "name": "read-receipt-msg",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev -w frontend\"",
    "dev:backend": "python backend/main.py",
    "build": "npm run build -w frontend",
    "start": "npm run build -w frontend && python backend/main.py"
  },
  "workspaces": ["frontend"],
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
