{
  "name": "read-receipt-msg",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev -w client\"",
    "dev:server": "go -C server run .",
    "build": "npm run build -w client",
    "start": "npm run build -w client && go -C server run ."
  },
  "workspaces": ["client"],
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
